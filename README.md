
  # Simple website with backend

  This is a code bundle for Simple website with backend. The original project is available at https://www.figma.com/design/vYUbMUESRxng4ezJun4NDr/Simple-website-with-backend.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  